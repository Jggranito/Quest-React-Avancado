import styled from 'styled-components';

export const Button = styled.button`
    border-width: 0px;
    font-family: inherit;
    font-size: inherit;
    font-style: inherit;
    font-weight: inherit;
    line-height: inherit;
    padding: 0px;
`